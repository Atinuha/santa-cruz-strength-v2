# Visual convergence screenshots

Captured from the production build served statically at 1280px wide, and at
414px for the mobile frame.

One caveat, stated plainly: the backend for this repository needs MongoDB and a
seeded database, neither of which was available in this environment. The pages
below were prerendered and hydrated against a **stub API** that serves the real
route registry and the real business configuration, but placeholder article and
team records. Layout, palette, typography, motion and spacing are therefore
real; article titles, coach names and blog cover images in these frames are not.

Recapture these against the real backend before treating them as a record of the
production site.
